/**
 * Typed views of SIP headers required for transaction and Dialog routing.
 *
 * <p>The generic message model remains the lossless source of wire values;
 * typed values are parsed on demand and do not replace unknown headers.</p>
 */
package org.loomsip.message.header;
